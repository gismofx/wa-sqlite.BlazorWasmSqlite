{
  "name": "wa-sqlite.blazorwasmsqlite",
  "version": "0.0.0",
  "description": "JS Interop Service that wraps wa-sqlite JS WASM package. Private and never published - the npm convention for an unpublished package is 0.0.0, and a real-looking number here invites confusion with the NuGet package version, which is unrelated.",
  "private": true,
  "scripts": {
    "dev": "webpack --mode=development",
    "prod": "webpack --mode=production",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.1.8",
    "terser-webpack-plugin": "^5.3.10",
    "webpack": "^5.91.0",
    "webpack-cli": "^4.6.0"
  },
  "dependencies": {
    "@journeyapps/wa-sqlite": "^1.5.0"
  },
  "license": "ISC"
}
